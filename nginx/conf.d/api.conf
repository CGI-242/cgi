# Redirection HTTP -> HTTPS
server {
    listen 80;
    server_name cgi242.normx-ai.com;

    # Certbot challenge
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # Tout le reste -> HTTPS
    location / {
        return 301 https://$host$request_uri;
    }
}

# HTTPS
server {
    listen 443 ssl;
    server_name cgi242.normx-ai.com;

    ssl_certificate /etc/letsencrypt/live/cgi242.normx-ai.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/cgi242.normx-ai.com/privkey.pem;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;

    # Security headers (niveau server — appliques aux locations SANS add_header)
    include /etc/nginx/conf.d/security-headers.conf;

    root /var/www/cgi242;
    index index.html;

    # Proxy API vers le backend
    location /api {
        limit_req zone=api burst=50 nodelay;

        proxy_pass http://api:3003;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Swagger / docs API
    location /docs {
        proxy_pass http://api:3003;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # Health check backend
    location /health {
        proxy_pass http://api:3003;
    }

    # index.html — jamais mis en cache (pour charger les nouveaux bundles JS)
    location = /index.html {
        include /etc/nginx/conf.d/security-headers.conf;
        add_header Cache-Control "no-cache, no-store, must-revalidate" always;
        add_header Pragma "no-cache" always;
    }

    # Frontend statique (SPA)
    location / {
        try_files $uri $uri/ /index.html;
        include /etc/nginx/conf.d/security-headers.conf;
        add_header Cache-Control "no-cache" always;
    }

    # Assets avec hash dans le nom (immutable) — cache long
    location /_expo/static/ {
        include /etc/nginx/conf.d/security-headers.conf;
        expires 1y;
        add_header Cache-Control "public, immutable" always;
    }

    # Autres assets statiques — cache court
    location ~* \.(png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        include /etc/nginx/conf.d/security-headers.conf;
        expires 7d;
        add_header Cache-Control "public" always;
    }
}
